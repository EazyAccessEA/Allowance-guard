/** Base URL for MSW — must match `AllowanceGuardProvider` `baseUrl` in tests. */
export const TEST_API_BASE = 'http://127.0.0.1:9/api/v1'

/** Browser-safe test key (hooks run under jsdom). */
export const TEST_PUBLIC_KEY = 'ag_pub_testxxxxxxxxxxxxxxxxxxxxxxxx'
