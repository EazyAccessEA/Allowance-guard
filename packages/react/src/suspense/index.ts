/**
 * Suspense-mode variants of the @allowance-guard/react hooks.
 *
 * Planned for v0.2.0 (see §12 of the architecture plan). Suspense variants
 * use TanStack Query's `useSuspenseQuery` so consumers can render behind
 * `<Suspense>` boundaries and Error Boundaries without manual
 * `isLoading` / `isError` branches.
 *
 * This module is intentionally empty for v0.0.1 — shipping a stub now so
 * the `exports` map entry exists and can be extended without a breaking
 * package-shape change.
 */

export {}
